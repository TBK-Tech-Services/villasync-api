import { Router } from "express";

const router = Router();

// API endpoints
router.get("/", (req, res) => {
    
});  
router.post("/", (req, res) => {
    
});  
router.get("/:id", (req, res) => {
    
});  
router.put("/:id", (req, res) => {
    
});  
router.get("/:id/bookings", (req, res) => {
    
});  
router.get("/:id/revenue", (req, res) => {
    
});  
router.get("/:id/calender", (req, res) => {
    
});  

export default router;