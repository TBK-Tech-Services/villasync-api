import { Router } from "express";

const router = Router();

// API endpoints
router.get("/", (req, res) => {

});  
router.put("/:id/role", (req, res) => {

});  
router.post("/invite", (req, res) => {

});  

export default router;